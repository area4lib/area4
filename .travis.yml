language: python
dist: trusty
sudo: required
os: linux
git:
  quiet: false
notifications:
  email: false
branches:
  except:
  - python-2-supported
python: "3.7-dev"
# DO NOT MODIFY ABOVE THIS LINE

install:
  - "cd $TRAVIS_BUILD_DIR/requirements/"
  - "python3 -m pip install -r test.txt"

before_script:
  - "wget $WEBHOOK_WGET"
  - "chmod +x send.sh"
  - "cd $TRAVIS_BUILD_DIR"

script:
  - "pip install --upgrade https://github.com/area4lib/builds/raw/master/area4-2.0.dev0-py3-none-any.whl"
  - "python3 testmodule.py"
